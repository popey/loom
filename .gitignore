# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/
obj/
/loom
/loomctl
cmd/loomctl/loomctl
/agenticorp
/connectors-service
/loom-project-agent
.cursor/

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Arbiter specific
.arbiter.json
.arbiter_secrets
# .vscode/
arbiter-*

# Database
*.db

# Web UI build artifacts
web/dist/

# MkDocs build output
site/
loom-self/
agenticorp-self/
# Key store files (encrypted API keys — must never be committed)
.keys.json
**/.keys.json
*.keystore
*.keystore.json

# Runtime data
data/
logs/
loom.log
.loom.pid

# Local bootstrap (contains API keys and local endpoints)
bootstrap.local

# Claude Code local settings
.claude/

# Bead store runtime data (written by tests and live server).
# The root .beads/ is intentional project data and stays tracked.
internal/**/.beads/
app/**/.beads/
